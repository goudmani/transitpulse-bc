
ruff==0.6.9
pytest==8.3.3
pytest-cov==5.0.0
boto3==1.35.36
requests==2.32.3
gtfs-realtime-bindings==1.0.0
protobuf==4.25.4
# Parses .tf files for scripts/validate_hcl.py and scripts/validate_wiring.py,
# so the Terraform checks run with no Terraform binary and no AWS credentials.
# Capped below 5.x: the checker's unq() helper depends on 4.x returning block
# labels with their quotes attached.
python-hcl2>=4.3,<5
